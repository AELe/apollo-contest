**变道前，主车需观察目标车道后方来车情况，确认安全后完成变道。变道过程中需保持与前后车辆的安全距离，与后车保持安全跟车距离**

修改 `kSafeTimeOnSameDirection`为8

```cpp
if (same_direction) {
      kForwardSafeDistance =
          std::fmax(kForwardMinSafeDistanceOnSameDirection,
                    (ego_v - obstacle->speed()) * kSafeTimeOnSameDirection);
      kBackwardSafeDistance =
          std::fmax(kBackwardMinSafeDistanceOnSameDirection,
                    (obstacle->speed() - ego_v) * kSafeTimeOnSameDirection);
    }
```

当障碍物与自车行驶方向相同时，根据障碍物车速动态计算前后安全距离

```cpp
    if (HysteresisFilter(ego_start_s - end_s, kBackwardSafeDistance,
                         kDistanceBuffer, obstacle->IsLaneChangeBlocking()) &&
        HysteresisFilter(start_s - ego_end_s, kForwardSafeDistance,
                         kDistanceBuffer, obstacle->IsLaneChangeBlocking())) {
      reference_line_info->path_decision()
          ->Find(obstacle->Id())
          ->SetLaneChangeBlocking(true);
      ADEBUG << "Lane Change is blocked by obstacle" << obstacle->Id();
      return false;
    }
```

判断前方和后方障碍物离自车是否过近，如果过近则认为此时不可变道 `IsClearToChangeLane`函数返回false

```cpp
    (*path_bound)[i].l_lower.l = init_sl_state_.second[0] > curr_lane_left_width
                                     ? curr_lane_left_width + adc_half_width
                                     : (*path_bound)[i].l_lower.l;
    (*path_bound)[i].l_lower.l =
        std::fmin((*path_bound)[i].l_lower.l, init_sl_state_.second[0] - 0.1);
    (*path_bound)[i].l_upper.l =
        init_sl_state_.second[0] < -curr_lane_right_width
            ? -curr_lane_right_width - adc_half_width
            : (*path_bound)[i].l_upper.l;
    (*path_bound)[i].l_upper.l =
        std::fmax((*path_bound)[i].l_upper.l, init_sl_state_.second[0] + 0.1);
```

- 后方来车距离不足 → is_clear_to_change_lane_ = false
- 需要施加 禁区限制 ，强制自车在当前车道等待

后方来车不安全时，强制车辆在换道起点之前 待在当前车道内等待，直到安全时机到来才允许变道

```cpp
GetStartPointSLState();
if (!DecidePathBounds(&candidate_path_boundaries)) {  // ← 生成 path_bound
    return Status(ErrorCode::PLANNING_ERROR, "lane change path bounds failed");
}
```

DecidePathBounds() 内部调用 GetBoundaryFromLaneChangeForbiddenZone(&path_bound) 施加禁区限制

```cpp
if (!OptimizePath(candidate_path_boundaries, &candidate_path_data)) {
    return Status(ErrorCode::PLANNING_ERROR, "lane change path optimize failed");
}
```

传给路径优化器

```
bool LaneChangePath::OptimizePath(
    const std::vector<PathBoundary>& path_boundaries,  // ← 接收参数
    std::vector<PathData>* candidate_path_data) {
  
    for (const auto& path_boundary : path_boundaries) {  // ← 遍历每个边界
        // ...
        bool res_opt = PathOptimizerUtil::OptimizePath(
            init_sl_state_, end_state, ref_l, weight_ref_l, 
            path_boundary,      // ← 传给通用工具类
            ddl_bounds, jerk_bound, config, 
            &opt_l, &opt_dl, &opt_ddl);
    }
}
```

遍历每个边界

```cpp
bool PathOptimizerUtil::OptimizePath(
    const std::array<double, 3>& init_state,
    // ... 其他参数
    const PathBoundary& path_boundary,  // ← 接收 path_boundary
    // ...
    ) {
  
    const auto& lat_boundaries = path_boundary.boundary();  // ← 提取边界
    const size_t kNumKnots = lat_boundaries.size();
  
    // 创建优化问题
    PiecewiseJerkPathProblem piecewise_jerk_problem(kNumKnots, delta_s,
                                                    init_state.second);
    // ...
  
    // 设置 l 的上下界约束
    piecewise_jerk_problem.set_x_bounds(lat_boundaries);  // ← 关键！
    piecewise_jerk_problem.set_dx_bounds(...);
    piecewise_jerk_problem.set_ddx_bounds(...);
    piecewise_jerk_problem.set_dddx_bound(...);
  
    // 求解
    if (!piecewise_jerk_problem.Optimize()) {
        return false;
    }
    // ...
}
```

设置l的上下界约束

```cpp
void PiecewiseJerkPathProblem::CalculateAffineConstraint(
    std::vector<c_float>* A_data,
    // ...
    std::vector<c_float>* lower_bounds,
    std::vector<c_float>* upper_bounds) {
  
    // 第 1 组约束：l 变量的边界（共 N 个）
    for (int i = 0; i < n; ++i) {
        variables[i].emplace_back(constraint_index, 1.0);
        lower_bounds->at(constraint_index) = x_bounds_[i].first;   // ← l_lower.l
        upper_bounds->at(constraint_index) = x_bounds_[i].second;  // ← l_upper.l
        ++constraint_index;
    }
    // ...
}
```

```cpp
bool PiecewiseJerkProblem::FormulateProblem(OSQPData* data) {
    // ...
    data->l = CopyData(lower_bounds);   // ← 传给 OSQP 的下界
    data->u = CopyData(upper_bounds);   // ← 传给 OSQP 的上界
    // ...
    osqp_solve(data);                    // ← OSQP 求解
}
```