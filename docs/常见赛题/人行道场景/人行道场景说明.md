### 一、场景回放与赛题分析

**核心需求**：

1. 主车必须在人行道前 **2.0-2.5m** 内精确停车
2. 行人完全通过前保持静止
3. 行人通过后立即恢复行驶

**评判标准**：

| 情况              | 结果   |
| --------------- | ---- |
| 停止位置不在2.0-2.5m内 | 扣20分 |
| 未停车或超出停止线       | 0分   |

**场景回放**：

<img title="" src="https://apollo-studio-public.bj.bcebos.com/community/article/image/55521b3ef6a1d144f25c0499a61e0a4ad2311757" alt="" width="713">

**技术难点**：

- 停止距离精度控制（±0.5m）
- 行人状态实时检测（是否完全通过）
- 停车超时处理机制

**解决思路**：

1. 修改`crosswalk`插件配置参数
2. 调整`stop_distance`至目标范围
3. 优化`stop_timeout`避免误启动

---

### 二、整体技术流程图&相关插件解析

<img src="https://apollo-studio-public.bj.bcebos.com/community/article/image/ae12ae3162e86da90683a4030bf534730dd46b27" title="" alt="" width="729">

#### 核心插件解析

##### 1. 配置文件

**路径**：`profiles/default/modules/planning/traffic_rules/crosswalk/conf/default_conf.pb.txt`  
**关键参数**：

```
stop_distance: 1.0          
stop_strict_l_distance: 4.0 
stop_loose_l_distance: 5.0  
stop_timeout: 10.0          
```

配置参数解释： 

```
https://apollo.baidu.com/community/Apollo-Homepage-Document?doc=BYFxAcGcC4HpYIbgPYBtXIHQCMEEsATAV0wGNkBbWA5UyRFdZWVBEAU0hFgoIH0KyYqk58ATOFYA7KXikBzcSABOCAGZq8pPn2VERkcaWXJIkAO4JUAa3EAlAKIBBACIBZBztJTMoCqiA
```

##### ****【科普环节】SL坐标系详解：****

SL坐标系将道路分解为沿道路方向的S轴和垂直于道路方向的L轴

1. ****S坐标****：
- S坐标代表的是沿道路长度的位置，可以理解为沿道路中心线的行驶距离。从道路的起始点开始计算，沿车道中心向前延伸。这个值通常用于表示对象在道路上前进的多远，类似于道路上的里程标。
2. ****L坐标****：
- L坐标表示的是横向位置，即对象相对于车道中心线的侧向距离。正值通常表示障碍物位于车道中心线的右侧，负值则表示在左侧。这个参数对于判断车辆是否需要变道或者调整车道位置以避免障碍物非常有用。

##### 2. 核心代码逻辑

**行人检测**：

代码段用于检查障碍物是否在道路上并判断是否需要停车。首先，通过调用reference_line.IsOnRoad(obstacle.PerceptionSLBoundary())来判断障碍物是否在道路上。如果障碍物在道路上（is_on_road为true），则进一步检查障碍物的SL坐标中的s值（obstacle_sl_point.s()）是否大于车辆行驶结束边界的s值（adc_end_edge_s）。如果是，则判定需要停车（stop设为true），否则不需要停车。

```planning/traffic_rules/crosswalk.cc
 const bool is_on_road =
      reference_line.IsOnRoad(obstacle.PerceptionSLBoundary());
    if (is_on_road) {
      if (obstacle_sl_point.s() > adc_end_edge_s) {
        stop = true;
﻿
      }
```

**构建停车墙**：

此函数，可以根据人行横道的位置和配置的停止距离，生成停车决策，并将其应用于规划框架中的参考线信息。

```
    util::BuildStopDecision(virtual_obstacle_id, crosswalk_overlap->start_s,
     util::BuildStopDecision(
        virtual_obstacle_id, crosswalk_overlap->start_s,
        //人行道停止距离配置文件
        config_.stop_distance(), 
        //停止原因：人行道
        StopReasonCode::STOP_REASON_CROSSWALK,
        crosswalk_to_stop.second, Getname(), frame, reference_line_info);
```

**停车时长控制**：

如果障碍物的速度小于等于预设的停止速度（kMaxStopSpeed），则判断为需要停车。代码首先检查是否已经记录了该障碍物的停车时间戳，如果没有，则添加当前时间戳。如果已经存在停车时间戳，则计算当前时间与之前记录的时间戳之间的停车时间。如果停车时间超过了配置文件中设置的停止超时时间（config_.crosswalk().stop_timeout()），则将停车状态设为false，即车辆启动。

```
           if (obstacle_speed <= kMaxStopSpeed) {
          //当速度小于默认值的停止速度时时判为STOP
          if (crosswalk_stop_timer[crosswalk_id].count(obstacle_id) < 1) {
            // add timestamp
            ADEBUG << "add timestamp: obstacle_id[" << obstacle_id
                   << "] timestamp[" << Clock::NowInSeconds() << "]";
            crosswalk_stop_timer[crosswalk_id].insert(
                {obstacle_id, Clock::NowInSeconds()});
          } else {
            double stop_time = Clock::NowInSeconds() -
                               crosswalk_stop_timer[crosswalk_id][obstacle_id];
            ADEBUG << "stop_time: obstacle_id[" << obstacle_id << "] stop_time["
                   << stop_time << "]";
            //当停止时间大于配置文件的停止超时时，车辆启动
            if (stop_time >= config_.stop_timeout()) {
              stop = false;
            }
          }
        }
```

##### 

**模块协作流程**：

1. **感知模块**：检测行人位置（SL坐标）
2. **规划模块**：
   - 判断行人是否在道路上
   - 计算相对位置（s > adc_end_edge_s）
   - 生成STOP决策
   - 执行停车指令
3. *数据传递**：
- 行人SL坐标 → 规划模块
- STOP决策 → 控制模块

---

### 三、解题方法

#### 1.代码调参

初始化配置文件：

```
buildtool profile config init --package planning-traffic-rules-crosswalk --profile=default
aem profile use default
```

查看profile插件目录结构：

```
tree profiles/default/modules/planning/traffic_rules/crosswalk/
```

目录结构：

```
profiles/default/modules/planning/traffic_rules/crosswalk/
`-- conf
    `-- default_conf.pb.txt
```

**配置文件修改**：

在工作apollo_workspace工作目录找到profiles/default/modules/planning/traffic_rules/crosswalk/conf/default_conf.pb.txt配置文件，调整停止距离配置参数和停止超时实践，达到我们期望避让行人效果。

```
# 修改前
stop_distance: 1.0
stop_strict_l_distance: 4.0
stop_loose_l_distance: 5.0
stop_timeout: 10.0

# 修改后
stop_distance: 2.3          # 目标值（2.0-2.5m）
stop_strict_l_distance: 8.0  # 扩大检测范围
stop_loose_l_distance: 8.0   # 扩大检测范围
stop_timeout: 50.0           # 延长超时时间
```

**调参原则**：

1. `stop_distance`：从2.0开始测试，每次±0.1m微调
2. `stop_timeout`：设置足够大（50.0s）避免误启动
3. `stop_strict_l_distance`：扩大至8.0m确保检测覆盖

**推荐值**：

```
stop_distance: 2.3
stop_strict_l_distance: 8.0
stop_loose_l_distance: 8.0
stop_timeout: 50.0
```

#### 2.场景调试过程

**操作流程**：

1. 启动DreamView：
   
   `aem bootstrap start --plus`
   
   - 访问：[http://localhost:8888](http://localhost:8888)
   - 模式设置：`PnC Mode` + `Scenario_sim`

2. 修改配置文件：
   
   ```
   profiles/default/modules/planning/traffic_rules/crosswalk/conf/default_conf.pb.txt
   ```
   
   修改前
   
   ```
   stop_distance: 1.0
   stop_strict_l_distance: 4.0
   stop_loose_l_distance: 5.0
   stop_timeout: 10.0
   ```

3. 重启模块：
   
   - DreamView → Module Controller → 重启Planning模块

4. 测试验证：
   
   - 选择人行道场景
   - 观察停止位置（需在2.0-2.5m）
   - 验证行人通过后车辆启动
- 查看日志确认参数（代码）是否生效：
  
  可以在源码中打AINFO日志，修改源码modules/planning/traffic_rules/crosswalk/crosswalk.cc
  
  下载crosswalk的包
  
  ```
  buildtool install planning-traffic-rules-crosswalk
  ```
  
  ---

**问题解决方法**：

| 问题现象      | 解决方案                        |
| --------- | --------------------------- |
| 停止距离>2.5m | 减小`stop_distance`值（每次-0.1m） |
| 停止距离<2.0m | 增大`stop_distance`值（每次+0.1m） |
| 行人通过后不启动  | 检查`stop_timeout`是否过小        |
| 未检测到行人    | 增大`stop_strict_l_distance`值 |

**最终验证**：

- 停止位置：2.3m（符合2.0-2.5m要求）
- 行人通过后车辆正常启动
- 场景得分：80-100分
