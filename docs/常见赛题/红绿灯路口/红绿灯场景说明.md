### 一、场景回放与赛题分析

**核心需求**：

1. 主车必须在停止线前 **1.5-2.0m** 内精确停车
2. 红灯期间保持静止（不得超出停止线）
3. 绿灯后通过路口时速度不超过 6 m/s

**评判标准**：

| 情况              | 结果   |
| --------------- | ---- |
| 停止位置不在1.5-2.0m内 | 扣20分 |
| 超速（每超1m/s）      | 扣20分 |
| 超出停止线或红灯未停      | 0分   |

**技术难点**：

- 停车距离精度控制（±0.5m）
- 交通灯状态实时检测（红/绿灯切换）
- 路口区域速度限制实施
- 多阶段场景切换逻辑

**解决思路**：

1. 修改`traffic_light`插件停止距离参数
2. 新增`region_speed_limit`限速插件
3. 优化场景切换逻辑（APPROACH→INTERSECTION_CRUISE）

---

### 二、整体技术流程图&相关插件解析

![](https://apollo-studio-public.bj.bcebos.com/community/article/image/820ae0551a7143f9bdd93d0d9db0f7ca49f7bace)

#### 核心插件解析

##### 1. 配置文件

**路径**：`profiles/default/modules/planning/traffic_rules/traffic_light/conf/default_conf.pb.txt`  
**关键参数**：

```
enabled: true
stop_distance: 1.0
max_stop_deceleration: 4.0
```

**场景进入配置**：

**路径**：`profiles/default/modules/planning/scenarios/traffic_light_protected/conf/scenario_conf.pb.txt`

```
# 
start_traffic_light_scenario_distance: 5.0  # 5米处进入场景
```



##### 2. 核心代码逻辑

**红灯检测与停车决策**：

这段代码的作用是检查交通灯是否已经完成。它遍历已完成的交通灯重叠区域的ID列表，然后将当前交通灯的ID与这些ID进行比较。如果当前交通灯的ID在已完成的交通灯重叠区域的ID列表中，那么设置变量traffic_light_done为true，然后退出循环。

路径：` modules/planning/traffic_rules/traffic_light/traffic_light.cc`

```
    bool traffic_light_done = false;
    for (const auto& done_traffic_light_overlap_id :
         traffic_light_status.done_traffic_light_overlap_id()) {
      if (traffic_light_overlap.object_id == done_traffic_light_overlap_id) {
        traffic_light_done = true;
        break;
      }
    }
    if (traffic_light_done) {
      continue;
    }

```

可能有小伙伴会问停止墙是如何生成的呢？停止墙的标志是什么？

首先当我们的主车确认前方交通灯不为绿灯时时，会生成停止墙，停止原因为黄/红信号灯，

代码以及代码路径（节选）：

路径：`modules/planning/traffic_rules/traffic_light/traffic_light.cc`

函数的作用是生成一条停车决策，并将其添加到参考线信息中。当交通规则需要控制车辆停止时，例如信号灯指示红灯时，就会生成这样的停车决策。

```
 util::BuildStopDecision(
        virtual_obstacle_id, traffic_light_overlap.start_s,
        config_.stop_distance(), StopReasonCode::STOP_REASON_SIGNAL,
        wait_for_obstacles, Getname(), frame, reference_line_info);
```



**场景阶段切换原理**：

这段代码是一个交通灯保护阶段（TrafficLightProtectedStage）中的交通灯靠近阶段（TrafficLightProtectedStageApproach）的处理函数（Process）。首先，它遍历当前交通灯重叠区域的ID列表，并检查每个交通灯的颜色是否为绿灯。如果存在任何一个交通灯不是绿灯，那么设置变量traffic_light_all_done为false，然后退出循环。否则，将变量traffic_light_all_done设置为true。

如果所有交通灯都是绿灯，那么函数调用FinishStage()来结束该阶段，然后返回Stage::FINISHED状态。否则，返回Stage::RUNNING状态，表示该阶段仍在运行中。

路径：`planning/scenarios/traffic_light/protected/stage_approach.cc`

```
StageResult TrafficLightProtectedStageApproach::Process(
    const TrajectoryPoint& planning_init_point, Frame* frame) {
  // traffic_light_all_done 用于标记是否所有交通信号灯都已处理完毕
  bool traffic_light_all_done = true;
  
  // 遍历当前交通信号灯的重叠区域
  for (const auto& traffic_light_overlap_id :
       context->current_traffic_light_overlap_ids) {
    // 获取交通信号灯在参考线上的重叠信息
    PathOverlap* current_traffic_light_overlap =
        reference_line_info.GetOverlapOnReferenceLine(
            traffic_light_overlap_id, ReferenceLineInfo::SIGNAL);
    if (!current_traffic_light_overlap) {
      continue;
    }

    // 设置交通信号灯不具备交通优先权
    reference_line_info.SetJunctionRightOfWay(
        current_traffic_light_overlap->start_s, false);

    // 获取车辆前沿位置相对于交通信号灯停车线的距离
    const double adc_front_edge_s = reference_line_info.AdcSlBoundary().end_s();
    const double distance_adc_to_stop_line =
        current_traffic_light_overlap->start_s - adc_front_edge_s;
    auto signal_color = frame->GetSignal(traffic_light_overlap_id).color();

    // 检查车辆到停车线的距离是否小于最大有效停车距离
    if (distance_adc_to_stop_line > scenario_config.max_valid_stop_distance()) {
      traffic_light_all_done = false;
      break;
    }

    // 检查交通信号灯的颜色是否为绿色
    if (signal_color != TrafficLight::GREEN) {
      traffic_light_all_done = false;
      break;
    }
  }

  // 如果所有交通信号灯都已经处理完毕，则返回 FinishStage() 表示阶段已完成
  if (traffic_light_all_done) {
    return FinishStage();
  }

  // 否则，返回 result.SetStageStatus(StageStatusType::RUNNING) 表示阶段仍在运行中
  return result.SetStageStatus(StageStatusType::RUNNING);
}

```

当主车靠近红绿灯场景时，会进入Scenario 为TRAFFIC_LIGHT_PROTECTED的红绿灯场景或其他

<img title="" src="https://apollo-studio-public.bj.bcebos.com/community/article/image/1f3a9ebfaec74c9f47e72cec166cc96c52088a67" alt="" width="613">

**进入APPROACH的stage定义：**

这段代码是交通灯保护阶段（TrafficLightProtectedStage）中的交通灯靠近阶段（TrafficLightProtectedStageApproach）的处理函数（Process）

路径：`planning/scenarios/traffic_light/protected/stage_approach.cc`

```
StageResult TrafficLightProtectedStageApproach::Process(
    const TrajectoryPoint& planning_init_point, Frame* frame)
//APPROACHstage 正在运行
{
  return result.SetStageStatus(StageStatusType::RUNNING);

}
```

这段代码是交通灯保护阶段（TrafficLightProtectedStage）中的交通灯靠近阶段（TrafficLightProtectedStageApproach）的结束函数（FinishStage）。当交通灯靠近阶段的任务完成后，该函数会将next_stage_设置为ScenarioConfig::TRAFFIC_LIGHT_PROTECTED_INTERSECTION_CRUISE，表示进入交通灯保护阶段的下一个阶段TRAFFIC_LIGHT_PROTECTED_INTERSECTION_CRUISE。然后，它返回Stage::FINISHED状态，表示该阶段已经完成。

```
Stage::StageStatus TrafficLightProtectedStageApproach::FinishStage() 
//APPROACHstage 正在完成
{
//省略
  next_stage_ = "TRAFFIC_LIGHT_PROTECTED_INTERSECTION_CRUISE";
  //进入下一个stage TRAFFIC_LIGHT_PROTECTED_INTERSECTION_CRUISE
  return StageResult(StageStatusType::FINISHED);
  //APPROACH的stage完成
}
```

<img title="" src="https://apollo-studio-public.bj.bcebos.com/community/article/image/4976b80dac424f6a655e5eb2316d37616c5ee3a0" alt="" width="595">



**进入INTERSECTION_CRUISE的stage定义：**

这段代码是交通灯保护阶段（TrafficLightProtectedStage）中的交通灯保护交叉路口阶段（TrafficLightProtectedStageIntersectionCruise）的处理函数（Process）。首先，它调用stage_impl_.CheckDone()函数，检查是否已经通过了交叉路口。如果已经通过了交叉路口，那么将返回true，表示该阶段已经完成。如果该阶段已经完成，那么函数调用FinishStage()来结束该阶段，然后返回Stage::FINISHED状态。否则，返回Stage::RUNNING状态，表示该阶段仍在运行中。

路径：`planning/scenarios/traffic_light/protected/stage_intersection_cruise.cc`

```
StageResult TrafficLightProtectedStageIntersectionCruise::Process(
    const common::TrajectoryPoint& planning_init_point, Frame* frame) {
  bool stage_done = CheckDone(*frame, injector_->planning_context(), true);
   //定义交叉路口
  if (stage_done) 
  //如果通过交叉路口
  {
    return FinishStage();
    //INTERSECTION_CRUISE的stage完成
  }
  return result.SetStageStatus(StageStatusType::RUNNING);

  //进入INTERSECTION_CRUISE的RUNNING
}
```

该函数调用FinishScenario()函数，结束整个交通灯保护阶段（TrafficLightProtectedStage），并返回Stage::FINISHED状态，表示该阶段已经完成。

```
StageResult TrafficLightProtectedStageIntersectionCruise::FinishStage() {
  return FinishScenario();
  //INTERSECTION_CRUISE的stage完成
}
```



##### 3. 新增限速插件

**路口限速实现**（新增插件）：

相关新增限速插件方法请详见：

添加task插件：

添加traffic_rules插件：



---

### 三、解题方法

#### 1. 代码调参

**初始化配置文件**：

```
buildtool profile config init --package planning-traffic-rules-traffic-light --profile=default
aem profile use default
```

**修改停止距离**：

```DIFF
# traffic_light/conf/default_conf.pb.txt
- stop_distance: 1.0
+ stop_distance: 1.7  # 取1.5-2.0m中间值
```



**配置限速插件**：

注：本教程traffic_rules插件形式

配置修改：

**路径**：`region_speed_limit/conf/default_conf.pb.txt`  
**推荐参数**：

```
forward_buffer: 3.0
backward_buffer: 2.0
limit_speed: 6.0  # 符合≤6m/s要求
```

**插件注册**：

```
<!-- traffic_rule_config.pb.txt -->
rule {
  name: "REGION_SPEED_LIMIT"
  type: "RegionSpeedLimit"
}
```



**调参原则**：

| 参数            | 调整方向  | 效果     |
| ------------- | ----- | ------ |
| stop_distance | +0.1m | 增大停车距离 |
| stop_distance | -0.1m | 减小停车距离 |
| limit_speed   | ≤6.0  | 确保不超速  |

#### 2. 场景调试过程

**操作流程**：

1. 启动DreamView：`aem bootstrap start --plus`

2. 修改配置文件：
   
   ```
   profiles/default/modules/planning/traffic_rules/traffic_light/conf/default_conf.pb.txt
   profiles/default/modules/planning/conf/traffic_rule_config.pb.txt  # 添加并生效限速插件
   profiles/default/modules/planning/traffic_rules/region_speed_limit/conf/default_conf.pb.txt
   ```
   
   

3. 重启模块：DreamView → Planning模块

4. 测试验证：
   
   - 验证停车位置（1.5-2.0m）
   
   - 检查绿灯后通过速度（≤6m/s）
   
   - 查看运行日志

**问题解决方法**：

| 问题现象      | 解决方案                      |
| --------- | ------------------------- |
| 停止距离>2.0m | 减小stop_distance值（每次-0.1m） |
| 停止距离<1.5m | 增大stop_distance值（每次+0.1m） |
| 通过速度>6m/s | 检查limit_speed是否≤6.0       |
| 未触发限速     | 确认插件注册配置                  |

**最终验证指标**：

1. 停止位置：1.7±0.2m
2. 通过速度：≤6.0m/s
3. 场景得分：≥80分

---

### 关键修改总结

1. **停车精度**：通过`stop_distance:1.7`实现1.5-2.0m停车要求
2. **速度控制**：新增`region_speed_limit`插件设置`limit_speed:6.0`
3. **场景完整性**：
   - 5m处进入TRAFFIC_LIGHT_PROTECTED场景
   - APPROACH阶段完成红灯停车
   - INTERSECTION_CRUISE阶段执行限速通过



**路径**：`profiles/default/modules/planning/traffic_rules/traffic_light/conf/default_conf.pb.txt`  
**关键参数**：### 二、整体技术流程图&相关插件解析

#### 核心插件解析

##### 1. 配置文件

**路径**：`profiles/default/modules/planning/traffic_rules/traffic_light/conf/default_conf.pb.txt`  
**关键参数**：

YAML

`stop_distance: 1.0  # 需调整为1.5-2.0m`

**场景进入配置**：

YAML

`# scenarios/traffic_light_protected/conf/scenario_conf.pb.txt start_traffic_light_scenario_distance: 5.0  # 5米处进入场景`

##### 2. 核心代码逻辑

**红灯检测与停车决策**：

CPP

`// 检测非绿灯状态 if (signal_color != TrafficLight::GREEN) {   util::BuildStopDecision(     virtual_obstacle_id,      traffic_light_overlap.start_s,     config_.stop_distance(),  // 关键参数     StopReasonCode::STOP_REASON_SIGNAL,     ...); }`

**场景阶段切换**：

CPP

`// APPROACH → INTERSECTION_CRUISE 切换逻辑 Stage::StageStatus FinishStage() {   next_stage_ = "TRAFFIC_LIGHT_PROTECTED_INTERSECTION_CRUISE";   return StageResult(StageStatusType::FINISHED); }`

**路口限速实现**（新增插件）：

CPP

`reference_line->AddSpeedLimit(   pnc_junction_overlap.start_s - config_.forward_buffer(),   pnc_junction_overlap.end_s + config_.backward_buffer(),   config_.limit_speed()  // 设置为6.0 );`

##### 3. 新增限速插件配置

**路径**：`region_speed_limit/conf/default_conf.pb.txt`  
**推荐参数**：

YAML

`forward_buffer: 3.0 backward_buffer: 2.0 limit_speed: 6.0  # 符合≤6m/s要求`

**插件注册**：

XML

`<!-- traffic_rule_config.pb.txt --> rule {   name: "REGION_SPEED_LIMIT"   type: "RegionSpeedLimit" }`

---

### 三、解题方法

#### 1. 代码调参

**初始化配置文件**：

BASH

`buildtool profile config init --package planning-traffic-rules-traffic-light --profile=default aem profile use default`

**修改停止距离**：

DIFF

`# traffic_light/conf/default_conf.pb.txt - stop_distance: 1.0 + stop_distance: 1.7  # 取1.5-2.0m中间值`

**配置限速插件**：

YAML

`# region_speed_limit/conf/default_conf.pb.txt limit_speed: 6.0  # 严格匹配6m/s要求`

**调参原则**：

| 参数            | 调整方向  | 效果     |
| ------------- | ----- | ------ |
| stop_distance | +0.1m | 增大停车距离 |
| stop_distance | -0.1m | 减小停车距离 |
| limit_speed   | ≤6.0  | 确保不超速  |

#### 2. 场景调试过程

**操作流程**：

1. 启动DreamView：`aem bootstrap start --plus`

2. 修改配置文件：
   
   BASH
   
   `vim profiles/default/modules/planning/traffic_rules/traffic_light/conf/default_conf.pb.txt vim modules/planning/conf/traffic_rule_config.pb.txt  # 添加限速插件`

3. 重启模块：DreamView → Planning模块

4. 测试验证：
   
   - 验证停车位置（1.5-2.0m）
   
   - 检查绿灯后通过速度（≤6m/s）
   
   - 查看运行日志：
     
     BASH
     
     `grep 'traffic_light' /apollo/data/log/planning.INFO`

**问题解决方法**：

| 问题现象      | 解决方案                      |
| --------- | ------------------------- |
| 停止距离>2.0m | 减小stop_distance值（每次-0.1m） |
| 停止距离<1.5m | 增大stop_distance值（每次+0.1m） |
| 通过速度>6m/s | 检查limit_speed是否≤6.0       |
| 未触发限速     | 确认插件注册配置                  |

**最终验证指标**：

1. 停止位置：1.7±0.2m
2. 通过速度：≤6.0m/s
3. 场景得分：≥80分

---

### 关键修改总结

1. **停车精度**：通过`stop_distance:1.7`实现1.5-2.0m停车要求
2. **速度控制**：新增`region_speed_limit`插件设置`limit_speed:6.0`
3. **场景完整性**：
   - 5m处进入TRAFFIC_LIGHT_PROTECTED场景
   - APPROACH阶段完成红灯停车
   - INTERSECTION_CRUISE阶段执行限速通过
